# ライントレーサー上位者ミッション
- ### 一周できる車体から、説明できる制御システムへ -

## 0. この資料の目的

あなたのライントレーサーは、すでにコースを一周できる段階まで来ています。

しかし、実習としてはここで終わりではありません。

「一度だけ走った」だけでは、現場では完成とは言えません。  
これからは、次の力を身につけることを目標にします。

- 何度走らせても安定して走る
- 走行結果を記録する
- 設定値を変えて比較する
- センサー値やモーター出力を数値で考える
- なぜうまく走るのかを説明できる
- 他人が読んでも分かるコード・記録を残す

応用過程では、単に「動いた」で終わるのではなく、  
**測定し、比較し、改善し、説明する力** が重要になります。

---

## 1. 上位者ミッションの進め方

以下のミッションを、優先順位の高い順に進めます。

すべてを一度に行う必要はありません。  
先生と相談しながら、自分の進度に合わせて取り組んでください。

---

# ミッション1：3回連続完走を目指す

## 目的

一度だけ完走できても、それは偶然かもしれません。
現場では、機械やシステムが  **何度やっても同じように動くこと** が重要です。
このミッションでは、ライントレーサーの再現性を確認します。

## 目標

同じ設定で、3回連続完走すること。

## 条件

- 途中で手を触れたら失敗
- スタート位置を少しずつ変えてもよい
- 失敗した場合は、どこで失敗したかを記録する
- 設定値を変えた場合は、必ず記録する

## コース区間

| 区間 | 内容 |
|---|---|
| A | スタート直後の直線 |
| B | 第１コーナー |
| C | S字 |
| D | 最終コーナー |
| E | 最後の直線復帰 |

## 記録表

| 試行 | 完走 | タイム | 不安定だった場所・失敗場所 |
|---:|---|---:|---|
| 1 | ○ / × |  |  |
| 2 | ○ / × |  |  |
| 3 | ○ / × |  |  |
| 4 | ○ / × |  |  |
| 5 | ○ / × |  |  |

## 提出物

- 走行記録結果
- 3回連続完走できたかどうか
- 失敗しやすい区間
- 安定化のために変更した点

---
# ミッション2：ON/OFF制御からP制御へ
## 目的

今までのライントレースは、おそらく次のような考え方で動いています。

```python
if left_black:
    turn_left()
elif right_black:
    turn_right()
else:
    go_forward()
```

これは、センサーが
- 黒を見た
- 黒を見ていない
という判断で動く制御です。
この方式を **ON/OFF制御** と呼びます。
次の段階では、センサー値を数値として読み取り、  
**どれくらいズレているか** に応じてモーターの出力を変えます。
これを **P制御** と呼びます。

## 考え方

P制御では、左右センサーの値の差を使います。

```python
error = left_val - right_val
```

この `error` が、車体のズレを表します。さらに、ズレの大きさに応じて補正量を決めます。

```python
correction = Kp * error
```

ここで、`Kp` は曲がり方の強さを決める値です。

- `Kp` が小さい：曲がり方が弱い
- `Kp` が大きい：曲がり方が強い
- 大きすぎる：左右に振れやすくなる

---
## やること

1. 左右センサーのADC値を表示する
2. `error = left_val - right_val` を表示する
3. `correction = Kp * error` を計算する
4. 左右モーターのPWM値に反映する
5. 低速で走行テストする
6. `Kp` を変えて走り方を比較する

---

## 記録すること

| 実験番号 | BASE_SPEED | Kp | 完走 | タイム | 走り方のメモ |
|---:|---:|---:|---|---:|---|
| 1 |  |  | ○ / × |  |  |
| 2 |  |  | ○ / × |  |  |
| 3 |  |  | ○ / × |  |  |
| 4 |  |  | ○ / × |  |  |
| 5 |  |  | ○ / × |  |  |

---

## 提出物

- P制御を使ったプログラム
- `BASE_SPEED` と `Kp` の設定値
- 走行記録
- ON/OFF制御とP制御の違いの説明

---

# ミッション3：勘ではなくデータで調整する

## 目的

ライントレーサーは、いろいろな設定値によって走り方が変わります。

たとえば、

- モーターの速度
- センサーのしきい値
- `Kp`
- センサーの位置
- 電池の状態
- 車体の重心

などです。

このミッションでは、感覚で調整するのではなく、  **記録を取りながら比較する** ことを目標にします。

## 重要ルール

一度に変えてよい条件は、**1つだけ** です。

悪い例：

```text
速度も変えた
Kpも変えた
センサー位置も変えた
```

これでは、何が原因で良くなったのか分かりません。

良い例：

```text
今回はKpだけ変える
今回はBASE_SPEEDだけ変える
今回はセンサー位置だけ変える
```

## やること

1. 現在の設定で基準走行を行う
2. 変更する値を1つ決める
3. その値だけを変えて走行する
4. 完走できたか、タイム、走り方を記録する
5. どの設定が一番よかったか考察する

## 記録表

| 実験番号 | 変更した項目 | 設定値 | 完走 | タイム | 走り方のメモ |
|---:|---|---:|---|---:|---|
| 1 | なし | 基準値 | ○ / × |  |  |
| 2 | BASE_SPEED |  | ○ / × |  |  |
| 3 | BASE_SPEED |  | ○ / × |  |  |
| 4 | Kp |  | ○ / × |  |  |
| 5 | Kp |  | ○ / × |  |  |

## 提出物

- 走行実験表
- 最もよかった設定値
- その設定を選んだ理由
- 改善前後の比較

---

# ミッション4：センサーキャリブレーションの自動化

## 目的

RPR-220のセンサー値は、いつも同じとは限りません。

次のような条件で値が変わります。

- 照明
- 床の色
- センサーの高さ
- 車体の影
- 電池電圧
- センサーの個体差

そのため、白と黒のしきい値を固定するだけでは、うまく走らないことがあります。

このミッションでは、白と黒の値を測定して、  
しきい値を自動で決める仕組みを作ります。

## 考え方

白の上で測定した値を `white_value`、  
黒の上で測定した値を `black_value` とします。

その中間をしきい値にします。

```python
threshold = (white_value + black_value) // 2
```

左右のセンサーに個体差がある場合は、左右別々にしきい値を持たせます。

```python
left_threshold = (left_white + left_black) // 2
right_threshold = (right_white + right_black) // 2
```

## やること

1. 白の上で左右センサー値を測る
2. 黒の上で左右センサー値を測る
3. 中間値を計算する
4. 計算したしきい値で走行する
5. 手動で決めたしきい値と比較する

## 記録表

| センサー | 白の値 | 黒の値 | 自動しきい値 |
|---|---:|---:|---:|
| 左 |  |  |  |
| 右 |  |  |  |

## 提出物

- 白値・黒値の測定結果
- 自動計算されたしきい値
- 手動しきい値との比較
- 自動キャリブレーションの手順説明

---
# ミッション5：ラインを見失ったときの復帰処理

## 目的

走行中に、左右両方のセンサーが白になることがあります。

これは、ラインを完全に見失った状態かもしれません。

そのまま前進し続けると、コースアウトします。

このミッションでは、最後にどちら側でラインを見たかを記憶し、  
ラインを見失ったときに探しに行く処理を作ります。

## 考え方

最後にラインを見た方向を変数に保存します。

```python
last_seen = "left"
```

左センサーが黒を検出したら、

```python
last_seen = "left"
```

右センサーが黒を検出したら、

```python
last_seen = "right"
```

両方白になったときは、`last_seen` を見て探しに行きます。

## 動作例

| 状態 | 動作 |
|---|---|
| 左が黒 | 左側にラインがあると記録 |
| 右が黒 | 右側にラインがあると記録 |
| 両方白 | 最後に見た方向へ探しに行く |
| 一定時間見つからない | 停止する |


## やること

1. `last_seen` 変数を作る
2. 左右どちらでラインを見たか記録する
3. 両方白になったときの動作を作る
4. 復帰できるかテストする
5. 復帰できた場合とできなかった場合を記録する

## 提出物

- `last_seen` を使ったプログラム
- 見失い時の動作説明
- 復帰成功例・失敗例
- 改良前後の比較

---

# ミッション6：コードを読みやすく整理する

## 目的

プログラムが長くなると、どこに何が書いてあるか分かりにくくなります。

応用過程では、他人が読めるコード、後から修正できるコードが重要になります。

このミッションでは、ライントレーサーのプログラムを整理します。

## やること

まずは、機能ごとに関数を分けます。

- モーターを動かす関数
- センサーを読む関数
- しきい値を決める関数
- ライントレースする関数
- 停止する関数

余裕があれば、ファイルを分けます。

```text
main.py
motor.py
sensor.py
config.py
```

## ファイルの役割例

| ファイル | 役割 |
|---|---|
| `main.py` | 全体の流れ |
| `motor.py` | モーター制御 |
| `sensor.py` | センサー読み取り |
| `config.py` | ピン番号、速度、Kpなどの設定値 |

## 提出物

- 整理したプログラム
- 関数一覧
- それぞれの関数の役割説明
- 改修前後の比較

---
# ミッション7：センサー位置で走りがどう変わるか調べる

## 目的

同じプログラムでも、センサーの位置によって走り方は変わります。

- センサーが前にある
- センサーが後ろにある
- センサーの高さが違う
- センサーが傾いている

これらは走行に大きく影響します。

このミッションでは、センサー位置を変えて走行結果を比較します。

## やること

1. 現在のセンサー位置を写真に撮る
2. センサー位置を少し前に出す
3. 走行結果を記録する
4. センサー位置を少し後ろに下げる
5. 走行結果を記録する
6. 最も安定した位置を考える

## 記録表

| センサー位置 | 直線 | Uターン | S字 | コメント |
|---|---|---|---|---|
| 前寄り |  |  |  |  |
| 標準 |  |  |  |  |
| 後ろ寄り |  |  |  |  |

## 提出物

- センサー位置の写真
- 走行結果
- 最終的な取り付け位置
- その位置を選んだ理由

---
# ミッション8：車体チェックリストを作る

## 目的

走行前には、車体が安全に動く状態か確認する必要があります。

これは、製造・保全・品質管理にもつながる考え方です。

## チェック項目例

| 項目 | OK |
|---|---|
| 電池ボックスが固定されている |  |
| 3.3V電源基板のOUTが約3.3Vである |  |
| GNDが共通になっている |  |
| 配線がタイヤに触れていない |  |
| センサー基板が傾いていない |  |
| モータードライバが固定されている |  |
| ネジがゆるんでいない |  |
| BL-13にゴミが入っていない |  |

## やること

1. 自分の車体用のチェックリストを作る
2. 走行前にチェックする
3. 問題があれば改善する
4. 改善前後の写真を残す

## 提出物

- 自作チェックリスト
- 点検結果
- 改善した箇所の写真

---
# ミッション9：他学生のサポートをする

## 目的

応用過程や現場では、自分が理解するだけでなく、他人に説明する力も必要になります。

ただし、他の学生の作業を代わりにやってはいけません。

## してよいこと

- 配線ミスを一緒に探す
- テスターの当て方を説明する
- GND共通を確認する
- はんだブリッジを指摘する
- センサーの向きを確認する
- どの資料を見るべきか案内する

## してはいけないこと

- 代わりにはんだ付けする
- 代わりにコードを書く
- 代わりに配線する
- 答えをそのまま言う

## 提出物

- 誰に何を説明したか
- 自分が説明して難しかった点
- 相手が自力でできるようになった点

---

# 進め方

次の順番を推奨します。

| 回 | ミッション | 主な成果物 |
|---:|---|---|
| 1 | ミッション1：3回連続完走 | 走行記録、失敗箇所 |
| 2 | ミッション2：P制御 | P制御プログラム |
| 3 | ミッション3：パラメータ調整 | 比較表、ベスト設定 |
| 4 | ミッション4または5：自動キャリブレーション／復帰処理 | 改良プログラム |
| 5 | ミッション6：コード整理と最終まとめ | 最終コード、技術レポート |

---

# 最終提出物

上位者ミッションに取り組んだ学生は、最後に次の内容をまとめて提出します。

## 1. 車体の写真

- 上から
- 横から
- センサー部分
- 配線の様子

## 2. 最終設定値

| 項目 | 値 |
|---|---:|
| BASE_SPEED |  |
| Kp |  |
| 左しきい値 |  |
| 右しきい値 |  |

## 3. 走行記録

| 試行 | 完走 | タイム | メモ |
|---:|---|---:|---|
| 1 | ○ / × |  |  |
| 2 | ○ / × |  |  |
| 3 | ○ / × |  |  |

## 4. 改良した点

- 何を変更したか
- なぜ変更したか
- 変更した結果どうなったか

## 5. 最終コード

- `main.py`
- 必要に応じて `motor.py`
- 必要に応じて `sensor.py`

## 6. 考察

次の問いに答えること。

1. 一番効果があった改良は何か。
2. その改良で、なぜ走りが良くなったと考えられるか。
3. まだ残っている課題は何か。
4. さらに改良するなら、次に何をするか。

---

# 最後に

一周できたことは、大きな成果です。

しかし、応用過程では、  
「動いた」だけではなく、  
**なぜ動いたか、どう改善したか、どの条件で安定するか** を説明する力が求められます。

このミッションでは、ライントレーサーを使って、  
制御・計測・実験・改善・説明の力を身につけてください。
