# 第５回：Pico 2 への移行と SoC アーキテクチャの比較
## 〜ハードが変わっても、Python（技術）は裏切らない〜

### 🎯 本日の目標
1.  **世界標準を知る**: Androidスマホの心臓部「ARM」の設計思想とビジネスモデルを学ぶ。
2.  **新プラットフォームへの適応**: Raspberry Pi Pico 2 WH の強みを理解し、環境を移行する。
3.  **ファームウェアの正体**: マイコンの中に「通訳さん（インタープリタ）」を導入する。
4.  **ポータビリティの証明**: 最小限の修正でコードを移植し、「Pythonの汎用性」を体感する。

---

## 📘 1. 理論のスパイス：脳みその設計図「ARM」と SoC

### 1.1 ARM社：工場を持たない最強の頭脳集団

* **設計図を売るビジネス**: 英国ARM社はチップを1枚も作りません。そもそも工場を持っていません。売っているのは「設計図（IPライセンス）」だけ。Qualcomm（Snapdragon）や MediaTek はこの設計図をライセンスしてチップを製造しています。
* **Androidスマホの標準**: 君たちの **Androidスマートフォン** の脳みそも、この ARM 系統です。
* **数奇な運命**: かつてソフトバンクが約3.3兆円で買収し、一時はソフトバンクの子会社でした。現在は米国で再上場。「中立な標準」として世界中のメーカーに利用されています。

### 1.2 Pico 2 (RP2350) の現場目線のメリット
* **スリム設計**: 横幅が狭い！ブレッドボードに刺しても両側に配線スペースがあり、扱いやすい。
* **2コア（デュアルコア）**: 脳みそが2つあり、最大150MHzで高速に動作する。
* **豊富な GPIO**: 自由に使えるピンが多く、将来の拡張性も安心。

---

## 📘 2. 実行方式の正体：なぜマイコンで Python なのか？

マイコンは本来「0 と 1」しか理解できません。Pythonを実行できるのは、中に**「通訳さん（インタープリタ）」**を住まわせるからです。

* **コンパイラ（C言語など）**: 実行前にPCで翻訳。速いが、修正のたびに翻訳が必要で手間がかかる。
* **インタープリタ（Python）**: マイコン内の「ファームウェア」が1行ずつ読み取って即実行。
* **エンジニアの武器**: 「Pythonさえ押さえておけば、ハードが変わっても同じ言葉で命令できる」。これが現代のエンジニアにとって最強のポータビリティ（移植性）になります。

---

## 📘 3. 実習：Pico 2 への「通訳さん」導入手順

### 3.1 物理的な接続（BOOTSEL モード）
1.  USBケーブルの片側だけをPCに挿しておく。
2.  Pico 2 の**白いボタン（BOOTSEL）を指で押し続ける**。
3.  **押したまま** USBを接続し、PCが「USBメモリ（RPI-RP2）」として認識したらボタンを離す。

### 3.2 インストール
1.  先生が配布したファイル（`.uf2`）を「RPI-RP2」ドライブへドラッグ＆ドロップ。
2.  コピー完了後、ドライブが勝手に閉じれば成功です。

### 3.3 疎通確認：マイコンと「対話」してみよう
1.  Thonny右下の設定を **「MicroPython (Raspberry Pi Pico)」** に変更。
2.  下の **Shell** に `>>>` と表示されたら、直接以下を打ち込んで Enter！
    ```python
    >>> 1 + 1
    2
    >>> print("Hello Pico 2")
    ```

---

## 📘 4. 移行確認：Pico 2 版「Lチカ」
物理ポートを制御できるか確認します。（オンボードLED：**GP25**）
```python
from machine import Pin
import time
led = Pin(25, Pin.OUT)
while True:
    led.toggle()
    time.sleep(0.5)
```

---

## 📘 5. 演習：ADC コードの移植（ポーティング）

**【ミッション】**
第4回（ESP32）で作ったプログラムを Pico 2 用に改造せよ。

* **アジャスト点**: 
    MicroPythonの標準仕様では、ADCの数値を **0〜65535 (16bit)** に抽象化します。
    * **入力**: `adc.read_u16()` (0〜65535)
    * **出力**: `pwm.duty_u16()` (0〜65535)

```python
# --- 改造のポイント ---
def get_duty(adc_val):
    # 分母を 65535 に書き換える。
    # 「比率」という考え方で設計していれば、修正はここだけ！
    hiritsu = adc_val / 65535
    duty = int(hiritsu * 65535)
    return duty
```

---

### 💡 エンジニアの知恵袋
「Androidスマホも、このPico 2も、根っこは同じARM。そしてどちらも Python が動く。エンジニアの仕事は『特定の機械の操作』を暗記することではなく、**『比率や正規化』のような共通する考え方**を身につけて、新しい道具に素早く適応することなんだよ。」

---

完璧な流れです！先生の配布する「魂（ファーム）」が、学生たちの技術をアップデートする鍵になりますね。応援しています！