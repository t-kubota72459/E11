# 第6回：ON/OFF制御による基本ライントレース

## 今日の目標

今回は、左右のRPR-220センサーとモーター制御を組み合わせて、ライントレーサーを走らせます。

まずは難しい制御ではなく、次のような単純な場合分けで動かします。

* 左センサーが黒なら左へ曲がる
* 右センサーが黒なら右へ曲がる
* どちらも黒でなければ直進する

このような制御を、ここでは **ON/OFF制御** と呼びます。

---

## 今日の流れ（200分）

| 時間 | 内容 |
|---:|---|
| 0〜20分 | 前回のしきい値確認 |
| 20〜45分 | センサー2個による場合分けを考える |
| 45〜80分 | 基本ライントレースプログラムを書く |
| 80〜120分 | 直線コースで低速テスト |
| 120〜155分 | ゆるいカーブでテスト |
| 155〜180分 | 速度と旋回の強さを少し調整 |
| 180〜200分 | 失敗パターンを記録 |

---

## 1. 前回までにできていること

これまでに、次のことを行いました。

* `motor.py` でモーターを動かせる。
* 左右のセンサー値を読める。
* 左右それぞれのしきい値を決めた。
* `is_left_black()`、`is_right_black()` で白黒を判定できる。

今回は、これらを1つのプログラムにまとめます。

---

## 2. センサーと動きの関係

今回のライントレーサーでは、左右2個のセンサーで黒いラインを見ます。

| 左センサー | 右センサー | 車体の状態 | 動作 |
|---|---|---|---|
| 白 | 白 | ラインが中央付近 | 直進 |
| 黒 | 白 | ラインが左側にある | 左へ曲がる |
| 白 | 黒 | ラインが右側にある | 右へ曲がる |
| 黒 | 黒 | 太い線、交差、ずれ | 低速直進または停止 |

まずは、この表どおりに動かしてみます。

---

## 3. main.py を作る

`motor.py` は第4回で作ったものを使います。

次の内容で `main.py` を作ります。

```python
from machine import ADC, Pin
import time
import motor

# ===== センサー設定 =====
left_sensor = ADC(Pin(26))
right_sensor = ADC(Pin(27))

# 自分の車体で測った値に変更する
LEFT_THRESHOLD = 30000
RIGHT_THRESHOLD = 30000

# ===== 走行パラメータ =====
BASE_SPEED = 25000
TURN_SPEED = 25000


def is_left_black():
    val = left_sensor.read_u16()
    return val > LEFT_THRESHOLD


def is_right_black():
    val = right_sensor.read_u16()
    return val > RIGHT_THRESHOLD


while True:
    left_black = is_left_black()
    right_black = is_right_black()

    if left_black and not right_black:
        # ラインが左にあるので左へ曲がる
        motor.turn_left(TURN_SPEED)

    elif right_black and not left_black:
        # ラインが右にあるので右へ曲がる
        motor.turn_right(TURN_SPEED)

    elif left_black and right_black:
        # 両方黒の場合はいったんゆっくり前進
        motor.forward(18000)

    else:
        # 両方白なら直進
        motor.forward(BASE_SPEED)

    time.sleep_ms(10)
```

---

## 4. まずはタイヤを浮かせてテストする

いきなり床で走らせず、最初はタイヤを浮かせてテストします。

確認すること：

* 左センサーを黒にすると、左へ曲がる動作になるか。
* 右センサーを黒にすると、右へ曲がる動作になるか。
* 両方白で、左右モーターが前進するか。
* `Ctrl+C` で止められるか。

---

## 5. 床で低速テストする

次に、床で低速テストします。

最初は、速さを上げすぎないようにします。

```python
BASE_SPEED = 20000
TURN_SPEED = 22000
```

速く走ることよりも、まずはラインを追えることを優先します。

---

## 6. 調整する値

うまく走らない場合は、次の値を調整します。

| 値 | 役割 | 大きくすると |
|---|---|---|
| `LEFT_THRESHOLD` | 左センサーの白黒判定 | 黒と判定しにくくなる |
| `RIGHT_THRESHOLD` | 右センサーの白黒判定 | 黒と判定しにくくなる |
| `BASE_SPEED` | 直進速度 | 速くなるが外れやすい |
| `TURN_SPEED` | 曲がるときの速度 | 旋回が強くなる |

---

## 7. よくある失敗

| 失敗 | 原因の例 | 対策 |
|---|---|---|
| ラインから飛び出す | 速度が速すぎる | `BASE_SPEED` を下げる |
| カーブで曲がりきれない | 旋回が弱い | `TURN_SPEED` を調整する |
| 左右にガタガタ揺れる | 旋回が強すぎる | `TURN_SPEED` を下げる |
| まったく反応しない | しきい値が合っていない | センサー値を再測定する |
| 逆に曲がる | 左右センサーまたは左右モーターが逆 | 配線とプログラムを確認する |

---

## 8. 今日の記録

| 項目 | 値・メモ |
|---|---|
| `LEFT_THRESHOLD` | |
| `RIGHT_THRESHOLD` | |
| `BASE_SPEED` | |
| `TURN_SPEED` | |
| 直線での様子 | |
| カーブでの様子 | |
| 失敗しやすい場所 | |

---

## 9. チェック課題

次のどちらかを達成してください。

* 直線コースを30cm以上、自動で進める。
* ゆっくりでよいので、黒ラインを追ってカーブに入れる。

成功したら、設定値と動作の様子を記録します。

---

## 10. 今日のまとめ

今日学んだことは、次の3つです。

1. 左右2個のセンサーで、ラインがどちらにあるか判断できる。
2. 場合分けを使うと、基本的なライントレースができる。
3. 速くする前に、センサー値・しきい値・速度を調整する必要がある。

次回は、ON/OFFだけでなく、センサー値の差を使って「曲がる強さ」を変える方法に進みます。
