# MicroPython でモーター制御

これまでの実習では、LEDや各種センサーなど、マイコンのピン（GPIO）から供給できるごくわずかな電力で動作するコンポーネントを扱ってきました。

ここでは、マイコンのピンから直接動かすことができない **「DCモーター」** のような大電力を必要とするアクチュエータを、マイコンを使って安全かつ自由自在（加減速・正転・逆転）に制御する方法を学びます。

---

# 1. DC モーターの特性と駆動の課題

DC（直流）モーターは、2つの端子に電圧を加えると回転する非常にシンプルなアクチュエータです。しかし、マイコンで制御する回路を組むには、2つの大きな壁があります。

### ① マイコンのピンから大電流は流せない

モーターが回転を始める瞬間（起動電流）や負荷がかかったときには、数百mA以上の大きな電流が流れます。
一方、Raspberry Pi Pico 2 WH の GPIO ピン1本から安全に出力できる電流は最大でも **数mA〜十数mA 程度** です。ピンに直接モーターを接続すると、確実にマイコンが破損します。

### ② 正転と逆転（方向切り替え）の難しさ

モーターの回転方向を逆にするには、2つの端子に加える **電圧の向き（極性）を反転** させる必要があります。GPIOピンのON/OFF（3.3VとGND）だけでは、回路のショートを引き起こす危険があり、単純には切り替えられません。

---

# 2. Hブリッジ回路とモータードライバ

上記2つの課題（大電流の制御・極性の反転）を同時に解決するのが、 **「Hブリッジ」** と呼ばれる回路です。4つのスイッチを H 型に配置し、対角線上のスイッチを同期してON/OFFすることで、モーターに流れる電流の向きを制御します。

実習では、このHブリッジ回路を1つのICに収めた **モータードライバIC** を使用します。

<br/>
<div style="text-align: center;">
  <img src="https://akizukidenshi.com/img/goods/3/109169.jpg" width="30%">
  <br/>
  DCモーターの外観
</div>
<br/>

### モータードライバICの基本制御ルール

ドライバへの「入力信号（IN1, IN2）」の組み合わせによって、モーターの状態を以下のように制御します。

* **IN1 = High, IN2 = Low** →  【正転】（前進）
* **IN1 = Low,  IN2 = High** →  【逆転】（後退）
* **IN1 = High, IN2 = High** →  【ブレーキ】（急停止）
* **IN1 = Low,  IN2 = Low** →  【空回り】（停止）

---

# 3. PWM 制御による速度調整（調光から調速へ）

第5回でLEDの明るさを変えるために学んだ **PWM（パルス幅変調）** を、今回はモーターの速度制御（調速）に応用します。
モータードライバの「INピン」に対してPWM信号を入力することで、モーターに加わる平均電圧を変化させ、回転速度を滑らかにコントロールします。

> ⚠️ **重要（Pico 2 WH のPWM仕様）：**
> MicroPython (Pico 2) の PWM デューティ比は **16bit（0 〜 65535）** で指定します。

---

# 4. AD3 によるモーターの「電気的ノイズ」観測実験

DCモーターは、内部でブラシと整流子が物理的に接触・切り替えを繰り返しながら回転するため、回路全体に激しい **「電気的ノイズ（スパイクノイズ）」** を発生させます。このノイズが対策なしでマイコンの電源ラインに伝わると、マイコンが突然フリーズしたり、再起動（リセット）したりする原因になります。

前回使用した **AD3（Analog Discovery 3）** のオシロスコープ機能を使って、モーターがどれほど「Noisy（うるさい）」なのか、実際に確かめてみましょう。

### 実験手順

1. AD3 のオシロスコープの **1+（橙線）** をモータードライバの電源ピン（またはモーターの端子）、 **1-（橙/白線）** を回路の GND に接続します。
2. WaveForms ソフトウェアのオシロスコープを起動し、モーター回転中の電圧波形を観測・記録します。
3. 激しいギザギザのスパイクノイズが観測できたら、モーターの端子間に **積層セラミックコンデンサ（0.1μF程度）** を並列に接続し（パスコン）、もう一度波形を観測します。

---

# 5. 本日のミッション：2ボタン式・デジタル調速システム

2つのタクトスイッチを使った加減速システムを、2つのステップ（改良）に分けて構築します。

### 【回路の配線イメージ】

* **GP14**: タクトスイッチA（加速ボタン・プルアップ設定）
* **GP15**: タクトスイッチB（減速ボタン・プルアップ設定）
* **GP16 (PWM)**: モータードライバ IN1
* **GP17**: モータードライバ IN2

---

## 【ステップ1】ボタンを押すごとに段階変速（カチカチ変速）

まずは、ボタンを1回押すごとに速度が一定量（5000ずつ）変化するプログラムを作成します。
以下のコードの `(  1  )` 〜 `(  3  )` の空欄を埋めて、プログラムを完成させなさい。

```python
import machine
import time

# ピンの設定
btn_up = machine.Pin(14, machine.Pin.IN, machine.Pin.PULL_UP)   # 加速ボタン
btn_down = machine.Pin(15, machine.Pin.IN, machine.Pin.PULL_UP) # 減速ボタン

# モーター制御ピン（IN1をPWMに指定、周波数 1kHz）
motor_in1 = machine.PWM(machine.Pin(16))
motor_in1.freq(1000)

motor_in2 = machine.Pin(17, machine.Pin.OUT)
motor_in2.value(0)   # IN2を0に固定（正転モード）

# 速度を管理する変数（0 〜 65535）
speed = 0
STEP = 5000  # 1回ボタンを押したときの変化量

print("Motor Control System Ready (Step 1)")

while True:
    # 加速ボタン（btn_up）が押された（0になった）ときの処理
    if btn_up.value() == 0:
        speed = speed + STEP
        if speed > (  1  ): # 最高速度を超えないように制限
            speed = (  1  )
        print("Speed UP:", speed)
        time.sleep_ms(200) # チャタリング・連続入力防止のための「長めの休み」
        
    # 減速ボタン（btn_down）が押された（0になった）ときの処理
    if btn_down.value() == 0:
        speed = speed - (  2  )
        if speed < 0:      # 最小値（停止）を下回らないように制限
            speed = 0
        print("Speed DOWN:", speed)
        time.sleep_ms(200)

    # 実際にモーターの速度を変更する
    motor_in1.duty_u16((  3  ))
    
    time.sleep_ms(20)
```

---

## 【ステップ2】押しっぱなしで滑らかに変速（長押しビューン変速）

ステップ1のプログラムでは、最高速度にするためにボタンを何度も連打（カチカチカチ！）しなければならず不便です。
そこで、 **「ボタンを押しっぱなし（長押し）にしている間、自動的に滑らかに加減速し続ける」** ようにプログラムを改良してください。

### 改良のヒント

* 連打防止用に設けていた `time.sleep_ms(200)`（0.2秒停止）の時間が長いと、押しっぱなしにしても次の処理まで待たされてしまいます。
* この `sleep` の時間を **適切な短い時間** に変更し、同時に1回あたりの変化量（`STEP`）を小さく調整することで、長押ししたときに「ビューン」と滑らかに速度が変わるようになります。

---

## 演習クイズ

1. 空欄 `(  1  )` に入る、Pico 2の16bit PWMにおける **最高速度を示すデューティ比の数値** はいくつか。
2. 空欄 `(  2  )` に入る、減速ボタンを押したときに引くべき変化量の変数名（または数値）は何か。
3. 空欄 `(  3  )` に入る、PWMの出力を決定するために渡す **速度データが格納された変数名** は何か。
4. **【プログラム改良問題】** ステップ2の「長押しで滑らかに変速する機能」を実現するために、ステップ1のコードのどこをどのように書き換えたか。変更した箇所（数値や変数など）を具体的に示しなさい。
5. **【レポート記述課題】** AD3 を使ってモーターの波形を観測した際、コンデンサ（パスコン）を入れる前と入れた後で、電圧波形（ノイズのギザギザ）にはどのような視覚的・数値的な違いが現れたか。実験で得られたスクリーンショットを添付し、その違いが生じた理由を説明せよ。
