# 第9回：最終調整・事前車検・レポート準備

## 今日の目標

次回はいよいよ、クラス内でのタイムトライアルとレポート作成です。

今回は、新しい機能を大きく追加する回ではありません。

今日の目標は、

* 本番で走れる状態に仕上げる
* 配線やセンサー位置を点検する
* プログラムを整理する
* レポートに書くデータを準備する

ことです。

---

## 今日の流れ（200分）

| 時間 | 内容 |
|---:|---|
| 0〜30分 | 事前車検：配線、電池、センサー、タイヤ |
| 30〜60分 | プログラム整理：変数名、コメント、不要コード削除 |
| 60〜100分 | 本番コース想定の試走 |
| 100〜130分 | 最後のパラメータ調整 |
| 130〜160分 | 模擬タイムトライアル |
| 160〜190分 | レポート用データ整理 |
| 190〜200分 | 第10回の準備物確認 |

---

## 1. 事前車検

本番前に、まず車体を点検します。

### 機械まわり

| チェック | OK |
|---|---|
| ギアボックスがしっかり固定されている | [ ] |
| ボールキャスターがスムーズに動く | [ ] |
| タイヤが外れかけていない | [ ] |
| 車体が大きく傾いていない | [ ] |
| センサー基板がぐらついていない | [ ] |

### 電気まわり

| チェック | OK |
|---|---|
| 電池ボックスの接触がよい | [ ] |
| Pico W、DRV8835、電池のGNDが共通 | [ ] |
| モーター線がタイヤに触れていない | [ ] |
| センサー線が抜けかけていない | [ ] |
| はんだ付け部分がショートしていない | [ ] |

### センサーまわり

| チェック | OK |
|---|---|
| RPR-220と床の距離が適切 | [ ] |
| 左右センサーの高さが大きく違わない | [ ] |
| センサーの下を配線が邪魔していない | [ ] |
| 白黒判定が正しくできる | [ ] |

---

## 2. プログラム整理

本番前に、プログラムを整理します。

### 残すべきもの

* `BASE_SPEED`
* `Kp`
* `LEFT_THRESHOLD`
* `RIGHT_THRESHOLD`
* センサー読み取り処理
* P制御の計算
* `motor.drive()`

### 消してよいもの

* 大量の `print()`
* 使っていない変数
* 古いテスト用コード
* コメントアウトしたままの長いコード

ただし、必要なコメントは残します。

---

## 3. 最終版 main.py の例

これは一例です。自分の車体に合わせて値を変更してください。

```python
from machine import ADC, Pin
import time
import motor

# ===== センサー設定 =====
left_sensor = ADC(Pin(26))
right_sensor = ADC(Pin(27))

# ===== 自分の車体に合わせた調整値 =====
LEFT_THRESHOLD = 30000
RIGHT_THRESHOLD = 30000
BASE_SPEED = 28000
Kp = 0.4


def limit(value, min_value, max_value):
    if value < min_value:
        return min_value
    elif value > max_value:
        return max_value
    else:
        return value


try:
    while True:
        left_val = left_sensor.read_u16()
        right_val = right_sensor.read_u16()

        error = left_val - right_val
        correction = int(Kp * error)

        left_speed = BASE_SPEED - correction
        right_speed = BASE_SPEED + correction

        left_speed = limit(left_speed, 0, 65535)
        right_speed = limit(right_speed, 0, 65535)

        motor.drive(left_speed, right_speed)

        time.sleep_ms(10)

except KeyboardInterrupt:
    motor.stop()
```

`Ctrl+C` で止めたときに、必ず `motor.stop()` するようにしています。

---

## 4. 最終設定値を記録する

| 項目 | 値 |
|---|---:|
| `LEFT_THRESHOLD` | |
| `RIGHT_THRESHOLD` | |
| `BASE_SPEED` | |
| `Kp` | |
| センサー高さ | |
| 電池の状態 | |

---

## 5. 模擬タイムトライアル

本番前に、模擬タイムトライアルを行います。

| 回 | タイム | 完走できたか | 失敗した場所・メモ |
|---:|---:|---|---|
| 1回目 | | | |
| 2回目 | | | |
| 3回目 | | | |

速い1回だけでなく、安定して完走できるかも大切です。

---

## 6. レポートに書く内容

第10回で作成するレポートには、次の項目を入れます。

1. 製作したライントレーサーの概要
2. 使用した部品
3. RPR-220センサー回路の仕組み
4. 左右センサーの白黒ADC値としきい値
5. DRV8835によるモーター制御
6. `motor.py` による関数化・モジュール化
7. ON/OFF制御の考え方
8. P制御の考え方
9. 調整したパラメータ
10. タイムトライアル結果
11. うまくいかなかった点と改善案
12. 考察

---

## 7. レポート用テンプレート

次の形で書き始めるとよいです。

```markdown
# ライントレーサー製作レポート

## 1. 目的

Raspberry Pi Pico W、RPR-220、DRV8835、タミヤのギアボックス等を用いて、黒いラインを自動で追従するライントレーサーを製作した。

## 2. 使用部品

| 部品 | 用途 |
|---|---|
| Raspberry Pi Pico W | 制御用マイコン |
| RPR-220 | 白黒判定用センサー |
| DRV8835 | モータードライバ |
| ツインモーターギアボックス | 左右モーター |
| ボールキャスター | 前輪または補助輪 |
| 単3電池4本 | モーター用電源 |
| NJM2396F33 | 3.3V電源生成 |

## 3. センサー回路

RPR-220は赤外LEDとフォトトランジスタを組み合わせた反射型フォトセンサーである。白い面では赤外線が反射し、黒い面では吸収される。この違いをADC値として読み取り、白黒を判定した。

## 4. センサー値としきい値

| 条件 | 左センサー | 右センサー |
|---|---:|---:|
| 白い床 | | |
| 黒いライン | | |
| しきい値 | | |

## 5. モーター制御

DRV8835を用いて左右のモーターを制御した。モーター制御の処理は `motor.py` にまとめ、`main.py` から呼び出す形にした。

## 6. ライントレース制御

最初にON/OFF制御を行い、その後、左右センサー値の差を使ったP制御を行った。

## 7. P制御

P制御では、左右センサー値の差を `error` とし、`correction = Kp * error` によって左右モーターの速度差を決めた。

## 8. 調整結果

| 項目 | 値 |
|---|---:|
| BASE_SPEED | |
| Kp | |
| LEFT_THRESHOLD | |
| RIGHT_THRESHOLD | |

## 9. タイムトライアル結果

| 回 | タイム | メモ |
|---:|---:|---|
| 1回目 | | |
| 2回目 | | |
| 3回目 | | |

## 10. 考察

うまくいった点、うまくいかなかった点、その原因、改善案を書く。

## 11. まとめ

今回の実習を通して、センサー、モーター、プログラム、機械調整が互いに関係していることを学んだ。
```

---

## 8. 考察に書いてほしいこと

感想だけで終わらせず、次のような内容を書きます。

* なぜその `BASE_SPEED` にしたのか。
* なぜその `Kp` にしたのか。
* ON/OFF制御とP制御で走り方はどう違ったか。
* 失敗しやすかった場所はどこか。
* その失敗の原因は何だと考えたか。
* 次に改善するとしたら、何を変えるか。

---

## 9. 第10回に向けた準備

第10回に必要なものを確認します。

| 準備物 | OK |
|---|---|
| 車体 | [ ] |
| Pico W | [ ] |
| 電池 | [ ] |
| USBケーブル | [ ] |
| 最終版 `main.py` | [ ] |
| 最終版 `motor.py` | [ ] |
| レポート用の記録 | [ ] |

---

## 10. 今日のまとめ

今日学んだことは、次の3つです。

1. 本番前には、プログラムだけでなく車体・配線・電池も確認する必要がある。
2. 調整値は、レポートで説明できるように記録しておく。
3. タイムだけでなく、失敗の原因と改善案を考えることが重要である。

次回は、タイムトライアルとレポート作成です。
