# 第1回：マイコン制御基礎 実習ガイド
**〜コンピュータの「理屈」を知り、Pythonで物理世界を動かす〜**

## 0. 開発環境のセットアップ（コックピット構築）

効率的な開発のため、2つの画面を以下のように使い分けます。

### ① 先生の画面をミラーリング（Google Meet）
* **[授業用 Meet リンク：https://meet.google.com/teh-esnx-svv](https://meet.google.com/teh-esnx-svv)**
* **左画面**に全画面表示。先生が「どこを見て、何に気づき、どう直したか」を盗んでください。

### ② マイコンとの対話（Thonny / Shell）

* **右画面**で Thonny を起動。
* 右下の設定を **[MicroPython (ESP32)]** にし、自分のCOMポートを選択。
* 下の **Shell (REPL)** 欄に `>>>` が出れば接続成功です。

---

## 1. ガイダンス：この授業の「目的」
* **脱・ラダー脳**：PLCのスキャン方式から、マイコンの「逐次実行」へ思考をシフトする。
* **2Qへの接続**：今学んでいるPythonは、次のクォーターで製作する「自律走行ロボット」の知能になります。

---

## 2. 理論：コンピュータを解剖する
### 2.1 ノイマン型（逐次実行）
コンピュータは、どんなに複雑な処理も「1行ずつ、順番に」しかできません。この「順番（時間の流れ）」を意識することが、バグを防ぐ第一歩です。

### 2.2 SoC（システム・オン・ア・チップ）
手元のESP32の銀色カバーの中には、CPU、メモリ、通信機能が凝縮されています。
* **設計図の種類**：iPhoneやPicoは「ARM」、ESP32は「Xtensa/RISC-V」。
* **抽象化の力**：設計図は違えど、Pythonを使えば同じ作法で操れます。

---

## 3. 【演習】Shell (REPL) で情報の裏側を覗く
ファイルを作る前に、Shellで直接命令を打って「デジタル情報の扱い」を体験します。

### 3.1 負の数の魔法「2の補数」
コンピュータには「引き算」の回路がありません。すべて足し算で処理します。
* **実験**：`-1` を8ビット（1バイト）のバイナリで覗いてみましょう。
```python
>>> bin(-1 & 0xff)
```
→ `0b11111111`（全部1）が `-1` を表す仕組み（2の補数）を、講義で解説します。

### 3.2 文字コード：数値と文字の橋渡し
コンピュータに文字という概念はありません。すべては「対応表」に基づいた数値です。
```python
>>> ord("A")    # 'A'の背番号（ASCII）は？
>>> ord("あ")   # 日本語（UTF-8）の背番号は？
>>> chr(12354 + 2) # 「あ」の2つ後ろの文字は？
```

---

## 4. 【実習】Lチカ：物理現象の制御
### 4.1 制限抵抗の計算
マイコンのピンを保護するため、オームの法則で抵抗値を求めます。
* $V_{cc} = 3.3V$ / $V_f = 2.0V$ / $I = 10mA$ (0.01A)
$$R = \frac{3.3 - 2.0}{0.01} = 130 \Omega$$
→ 安全のため、手元の **220 $\Omega$ または 330 $\Omega$** を使用します。

### 4.2 Shellからの直接介入
```python
from machine import Pin
led = Pin(2, Pin.OUT)
led.value(1)  # 点灯
led.value(0)  # 消灯
```

---

## 5. 【応用】自動実行プログラムの作成
以下のコードをエディタに書き、PCのデスクトップに `lesson1.py` として保存・実行してください。

```python
import utime
from machine import Pin

led = Pin(2, Pin.OUT)
t = 1.0  # 点滅間隔

while True:
    led.value(1)
    utime.sleep(t)
    led.value(0)
    utime.sleep(t)
    
    # 徐々に速くする
    if t > 0.05:
        t *= 0.8
        print("現在の点滅間隔(秒):", t)
```

---

## 6. 成果物の提出（本日の評価）
Google Classroom の **「第1回 課題提出フォーム」** にて、以下を完了してください。
1. **確認クイズへの回答**（ノイマン型、2の補数、文字コードについて）。
2. **作成したコード (`lesson1.py`) のアップロード**。

---

### アジャイルな次の一手：
第1回を終えた際、以下の感触を確認してみてください。
* **A：順調すぎる場合** → 第2回でビット演算を深掘りし、第3回でUART通信へ。
* **B：配線で苦戦した場合** → 第2回は物理入力（スイッチ）に時間を割き、理論は軽めにする。

UARTやLCDの「いつやるか」は、この第1回のリアクションを見てから決めるのが一番安全ですね。先生、まずは4/10のこの「スプリント1」に集中しましょう！