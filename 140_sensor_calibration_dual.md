# 第5回：左右センサー値の取得とキャリブレーション

## 今日の目標

今回は、第1回・第2回で作ったRPR-220のデュアルセンサー基板を、実際にライントレーサーの車体に取り付けます。

今日の目標は、次の3つです。

1. 左右2個のセンサー値をPico Wで読み取る。
2. 白い床と黒いラインで、数値がどのように変わるか確認する。
3. 左右それぞれの「しきい値」を決める。

今日はまだ自動走行はしません。

まずは、ロボットが「見ている値」を人間が確認します。

---

## 今日の流れ（200分）

| 時間 | 内容 |
|---:|---|
| 0〜20分 | センサー基板の目視確認 |
| 20〜45分 | Pico Wへの接続確認 |
| 45〜75分 | 左右ADC値を表示する |
| 75〜110分 | 白い床・黒いラインで値を記録する |
| 110〜140分 | 左右それぞれのしきい値を決める |
| 140〜170分 | `is_left_black()` / `is_right_black()` を作る |
| 170〜200分 | 手で車体を動かしながら判定確認 |

---

## 1. センサー基板の確認

作業に入る前に、次のことを確認します。

* RPR-220が左右に2個ついている。
* 3.3V、GND、左出力、右出力の4本が出ている。
* センサー基板が車体に固定されている。
* センサーが床に近すぎたり、遠すぎたりしない。
* センサーの下をタイヤや配線が邪魔していない。

RPR-220は、床から数mm程度の距離でよく反応します。高すぎると反応が弱くなります。

---

## 2. 配線

この資料では、次の接続を使います。

| センサー基板 | Pico W |
|---|---|
| 3.3V | 3V3 |
| GND | GND |
| 左センサー出力 | GP26 / ADC0 |
| 右センサー出力 | GP27 / ADC1 |

> ⚠️ 注意  
> Pico WのADC入力に5Vを入れてはいけません。センサー回路は3.3Vで動かします。

---

## 3. 左右のADC値を読む

まずは、左右のセンサー値を表示します。

```python
from machine import ADC, Pin
import time

left_sensor = ADC(Pin(26))
right_sensor = ADC(Pin(27))

while True:
    left_val = left_sensor.read_u16()
    right_val = right_sensor.read_u16()

    print("L:", left_val, " R:", right_val)
    time.sleep_ms(100)
```

`read_u16()` は、0〜65535の数値を返します。

値が大きいほど、センサー出力の電圧が高いということです。

---

## 4. 白と黒の値を記録する

車体を手で持ち、センサーの下に白い床と黒いラインを置いて値を記録します。

| 条件 | 左センサー値 | 右センサー値 |
|---|---:|---:|
| 白い床 | | |
| 黒いライン | | |
| 白と黒の中間 | | |
| 決めたしきい値 | | |

同じ部品を使っていても、左右のセンサー値が完全に同じになるとは限りません。

そのため、**左センサー用のしきい値**と**右センサー用のしきい値**を別々に決めても構いません。

---

## 5. しきい値とは

しきい値とは、白と黒を区別する境目の数値です。

たとえば、次のような値だったとします。

| 条件 | ADC値 |
|---|---:|
| 白い床 | 12000 |
| 黒いライン | 48000 |

この場合、真ん中あたりの 30000 をしきい値にできます。

```python
THRESHOLD = 30000
```

今回のRPR-220回路では、黒いラインのときに値が大きくなる想定です。

```python
if val > THRESHOLD:
    # 黒
else:
    # 白
```

---

## 6. 左右の白黒判定関数を作る

次のプログラムで、左右の白黒を判定します。

```python
from machine import ADC, Pin
import time

left_sensor = ADC(Pin(26))
right_sensor = ADC(Pin(27))

LEFT_THRESHOLD = 30000
RIGHT_THRESHOLD = 30000


def is_left_black():
    val = left_sensor.read_u16()
    return val > LEFT_THRESHOLD


def is_right_black():
    val = right_sensor.read_u16()
    return val > RIGHT_THRESHOLD


while True:
    left_black = is_left_black()
    right_black = is_right_black()

    print("left:", left_black, " right:", right_black)
    time.sleep_ms(100)
```

`True` は「黒」、`False` は「白」と考えます。

---

## 7. 車体を手で動かして確認する

車体をまだ走らせず、手でゆっくり動かします。

次の状態を確認しましょう。

| 状態 | 左判定 | 右判定 |
|---|---|---|
| 両方とも白い床 | False | False |
| 左センサーだけ黒ライン上 | True | False |
| 右センサーだけ黒ライン上 | False | True |
| 両方とも黒ライン上 | True | True |

この確認ができていないまま走らせると、原因不明の暴走に見えることがあります。

---

## 8. チェック課題

次の2つを先生に見せてください。

1. 左右のADC値を表示できる。
2. 車体を手で動かしたとき、左右の白黒判定が正しく変わる。

また、次の表を完成させます。

| 項目 | 左 | 右 |
|---|---:|---:|
| 白い床の値 | | |
| 黒いラインの値 | | |
| 自分で決めたしきい値 | | |

---

## 9. うまくいかないときの確認

| 症状 | 確認すること |
|---|---|
| 値がずっと0に近い | 3.3V、GND、出力線、はんだ |
| 値がずっと最大に近い | GND、センサー向き、負荷抵抗 |
| 白黒であまり変わらない | センサーの高さ、外光、抵抗値、ライン材質 |
| 左右が逆に表示される | 出力線の接続 |
| 手で触ると値が大きく変わる | 配線の接触不良 |

---

## 10. 今日のまとめ

今日学んだことは、次の3つです。

1. センサー値は0〜65535の数値として読める。
2. 白と黒を分けるには、しきい値を決める必要がある。
3. 左右のセンサーは、同じ部品でも値がずれることがある。

次回は、センサーの白黒判定を使って、いよいよライントレース走行を行います。
